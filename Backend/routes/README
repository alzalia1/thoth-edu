Ici se trouvent les fonctions à l'autre bout de l'API.

À l'ajout d'une route, ne pas oublier les étapes suivantes :

    1. Ajouter la route dans main.py (se référer au commentaire explicatif du début)
    2. Importer le fichier de route dans main.py (eh oui, pour utiliser la fonction, il faut l'avoir)
    3. Ne pas oublier de notifier alzalia de toutes les nouvelles libraries à télécharger sur le serveur

Un fichier de route devrait :

    1. Être nommé de la même façon que la route (api.thoth-edu.fr/example devrait être lié à example.py)
    2. Être doté d'une description de ce que fait la route en début de fichier, avec la notation """ """ (pour une meilleure utilisation sous VSCode)
